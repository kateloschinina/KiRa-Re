import mainImage from './bird-main-image.png';
import blurredImage from './bird-blurred-image.png';

const style = {
    top: '20vh', // from the top, % view height
    left: '20vw', // from the left, % view width
    width: '10vw' // width, % view width
}

export default {
    style,
    mainImage,
    blurredImage
};
